syntax = "proto3";

package backend;

service Backend {
  rpc Register(RegisterRequest) returns (RegisterReply) {
  }
}

message RegisterRequest {
  string email = 1;
  string password = 2;
}

message RegisterReply {
  string message = 1;
}
